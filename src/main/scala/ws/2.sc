import scala.collection.mutable.ArrayBuffer

def map(xs: Seq[Int], f: Int => Int): Seq[Int] = {
  ???
}

def filter(xs: Seq[Int], f: Int => Boolean): Seq[Int] = {
  ???
}

val buf = ArrayBuffer.empty[Int]
buf += 3
buf += 30
buf += 32

buf

Seq(1, 2, 3)
